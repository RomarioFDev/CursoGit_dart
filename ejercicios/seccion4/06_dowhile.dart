import 'dart:io';

main() {
  /**
   * En el do while, por lo menos se ejecutara una vez todo lo que
   * esta en el do, mientras que el while normal, no ejecutara
   * el ciclo hasta que se cumpla la condicion.
   */
}
